services:
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 123456
      MYSQL_DATABASE: doantotnghiep
    volumes:
      - ./data/mysql:/var/lib/mysql

    # docker compose -p system-management up -d